[package]
name = "umc-detect"
version = "1.0.0"
edition = "2021"
description = "UMC automatic format detection (magic bytes + heuristics)"

[dependencies]
umc-core  = { path = "../umc-core" }
tracing   = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
