[package]
name = "umc-validate"
version = "0.1.0"
edition = "2021"
description = "UMC validation (structural, numeric, round-trip) and conversion certificate"

[dependencies]
umc-core    = { path = "../umc-core" }
sha2        = { workspace = true }
hex         = { workspace = true }
serde       = { workspace = true }
serde_json  = { workspace = true }
tracing     = { workspace = true }
